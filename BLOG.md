## 가상돔 라이브러리의 key diff 에 LIS 알고리즘 적용하기

가상돔의 key-diff 방식의 성능을 개선하기 위해 LIS(최장 증가 부분 수열) 알고리즘 적용해 보았다.

내가 만든 가상돔의 key-diff 시스템에 알고리즘은 어디서 최적화된 알고리즘을 참고하여 만든 부분은 아었다.

키를 가지고 있는 리스트에서 같은 위치상에 새롭게 만들어지는 key 기반 리스트들을 교체 해줄때 dom 이동을 최소화 하도록 만드는 것이다.

dom의 위치를 항상 새로 꼽아주면.. 불필요하게. dom에서 빠졌다가 넣어지는 동작이 발생하면서.. 특히 사용자가 리스트 아이템에 애써 넣어놓은에 애니메이션이나 트랜지션 효과가 없어질 수 있기 때문이다.

기존 내가 생각한 알고리즘은 기존 키 기반의 리스트와 새로 만들어지 키 기반의 리스트에서 공통된 키를 뽑아내어 역순이 없으면 공통 교집합 키는 그대로 놔두고 새롭게 만들어진 아이템들만 새로운 위치에 꼽아주는 방식이었다.  교집합에서 기존 키의 순서와 조금이라도 틀리면 처음부터 모든 아이템들을 새로 꼽아주는 방식이다.

기존에 이렇게 만든 이유는.. 최소한의 코드로 가장 효과적인 구현을 하고 싶었기 때문이다. 보통의 가상돔의 키 기반의 리스트들을 사용자들이 사용할때 키 순서가 바뀌는 일은 흔하지 않기 때문에 이정도 구현이면 보통의 사용성에서는 충분하다고 생각하였고.. 적은 코드로 가장 일반적인 패턴에서 dom이동을 최소화 할수 있다고 생각했다.

충분히 정확하고 효과적이라고 생각했지만, 마음 한구석에는 항상 마음에 숙제처럼 불편함이 있었는데.

모든 키 기반 리스트에서 저 알고리즘을 구현하려면 O n^2 의 시간 복잡도를 갖게 된다. 

원래의 키 기반 리스트와 재생성시 만들어진 키기반 리스트에서 공통된 키를 뽑고... 역순이 없는지 다시 반복해서 체크하는 과정에서 쓸대없는 비교가 많이 만들어진다. 이 부분을 본능적으로 알고 있긴 했었지만... 구현단계에서는 다른 큰 문제도 많이 남아있었기 때문에 그 상태로 만족했다. 이 부분이 구현상 복잡한 부분이기도 해서 함부로 건드리기도 언짢았다.


그 후에도 LIS 라는 알고리즘을 알게 되었고, 내가 만든 가상돔에 적용해봐야 겠다는 생각을 하고 있었지만. 이 부분은 함부로 건드리면 기존에 최적화 되었던 구현의 정확도를 깨뜨릴수 있을꺼라고 생각되어 쉽게 손이 가지 않았다.

그ㅓ
