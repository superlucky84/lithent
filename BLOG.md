## 가상돔 라이브러리의 key diff 에 LIS 알고리즘 적용하기

내가 만든 가상돔 라이브러리의 key-diff 성능을 개선하기 위해 LIS(최장 증가 부분 수열) 알고리즘을 적용해 보았다.

목표는 하나다. **키 기반 리스트가 다시 그려질 때 DOM 이동을 최소화하는 것.** 결과부터 말하면 1만 개짜리 리스트에서 두 항목의 자리만 바꾸는 작업이 2,719ms 에서 83ms 로 줄었다.

### 왜 DOM 이동을 줄여야 하나

가상돔을 쓰는 사람은 보통 이런 코드를 쓴다.

```jsx
<ul>
  {items.map(item => <li key={item.id}>{item.text}</li>)}
</ul>
```

`items` 가 바뀌면 리스트를 다시 그려야 한다. 이때 모든 `<li>` 를 DOM 에서 빼냈다가 다시 꽂아도 화면 결과는 똑같아 보인다. 하지만 눈에 보이지 않는 부작용이 생긴다.

- 사용자가 애써 넣어둔 CSS 애니메이션이나 트랜지션이 끊긴다
- 입력 중이던 요소의 포커스가 풀린다
- `<iframe>`, `<video>` 같은 요소는 상태가 초기화된다

그래서 결과만 맞추는 게 아니라 **실제로 바뀐 것만 최소한으로 움직이는 것**이 중요하다.

### 내가 만들었던 알고리즘

내가 만든 가상돔의 key-diff 는 어디서 최적화된 알고리즘을 참고해서 만든 것이 아니었다. 직접 생각해낸 방식은 이랬다.

1. 기존 리스트와 새 리스트에서 **공통된 key 만 뽑아낸다**
2. 양쪽의 순서를 비교해서 **역순이 없으면** 공통 아이템은 그대로 두고, 새로 생긴 것만 제자리에 꽂는다
3. 순서가 **조금이라도 어긋나면** 처음부터 모든 아이템을 새로 꽂는다

상황별로 어떻게 동작하는지 정리하면 이렇다.

| 리스트에 생긴 변화 | 공통 key 순서 | 실제 DOM 이동 |
|---|---|---|
| 내용만 바뀜 `[1,2,3]` → `[1,2,3]` | 유지 | 없음 ✅ |
| 끝에 추가 `[1,2,3]` → `[1,2,3,4]` | 유지 | 추가분만 ✅ |
| 중간 삽입 `[1,2,3]` → `[1,9,2,3]` | 유지 | 삽입분만 ✅ |
| 일부 삭제 `[1,2,3,4]` → `[1,3]` | 유지 | 없음 ✅ |
| 두 개 자리 바꿈 `[1,2,3,4]` → `[1,4,3,2]` | 깨짐 | **전부** ❌ |
| 역순 정렬 `[1,2,3,4]` → `[4,3,2,1]` | 깨짐 | **전부** ❌ |

이렇게 만든 이유는 **최소한의 코드로 가장 효과적인 구현을 하고 싶었기 때문**이다. 표에서 보이듯 위쪽 네 가지가 실무에서 마주치는 대부분의 경우다. 목록에 항목을 추가하고, 지우고, 내용을 갱신하는 일 말이다. 반면 이미 그려진 리스트의 **순서가 뒤바뀌는 일은 흔하지 않다.**

그러니 이 정도 구현이면 보통의 사용성에서는 충분하다고 생각했다. 적은 코드로 가장 일반적인 패턴에서 DOM 이동을 최소화할 수 있다고 봤다.

### 마음 한구석에 숙제처럼 남아 있던 것

충분히 정확하고 효과적이라고 생각했지만, 찜찜한 구석이 둘 있었다.

**첫째, 시간 복잡도가 O(n²)였다.**

공통된 key 를 뽑아내고, 다시 역순이 없는지 반복해서 확인하는 과정에서 쓸데없는 비교가 잔뜩 만들어진다. 리스트가 커질수록 이 비용은 제곱으로 늘어난다. 이 부분을 본능적으로 알고는 있었지만, 구현 단계에서는 다른 큰 문제도 많이 남아 있었기 때문에 그 상태로 만족했다. 구현상 복잡한 부분이기도 해서 함부로 건드리기 언짢았던 것도 있다.

**둘째, 표의 아래 두 줄이 너무 비쌌다.**

순서가 어긋나면 전부 다시 꽂는다는 건, 1만 개짜리 리스트에서 **딱 두 개의 자리를 바꾸려고 1만 번의 DOM 이동을 한다**는 뜻이다. 흔하지 않은 경우라고 스스로를 설득했지만, 흔하지 않다고 해서 일어나지 않는 것은 아니다.

그 후에 LIS 라는 알고리즘을 알게 되었고 내 가상돔에 적용해봐야겠다는 생각을 계속 하고 있었다. 하지만 함부로 건드리면 기존에 어렵게 맞춰놓은 구현의 정확도를 깨뜨릴 수 있을 것 같아 쉽게 손이 가지 않았다.

### 그러다 크레딧이 생겼다

필자는 평소 업무에 클로드코드를 많이 활용하고 있는데, 어느 날 클로드코드에서 fable 사용 체험을 위한 무료 토큰을 나눠줬다. 그래서 이 크레딧으로, 오래 미뤄뒀던 key 기반 리스트의 리렌더링 성능 문제를 LIS 로 개선하는 작업에 손을 대보기로 했다.

이 글에서는 다음 순서로 이야기를 풀어보려 한다.

- 기존 구현이 어떻게 생겼었는지 (`chkDiffLoopOrder` 와 `'L'` 타입)
- LIS 가 무엇이고, 왜 이것이 DOM 이동 최소화와 같은 문제인지
- 실제 가상돔 코드에 어떻게 녹였는지 (`oi` 속성과 역순 배치)
- 그 결과 `'L'` 타입이 왜 통째로 필요 없어졌는지


## 기존 구현에는 'L' 이라는 표식이 있었다

본격적인 이야기로 들어가기 전에, 기존 코드에 이름을 몇 개 붙여둬야겠다. 앞으로 계속 등장할 이름들이다.

위에서 말한 "역순이 없으면 그대로 두고, 하나라도 어긋나면 전부 다시 꽂는다"는 판단은 코드상 두 조각으로 나뉘어 있었다.

첫째는 순서를 검사하는 `chkDiffLoopOrder` 함수다. 양쪽 리스트에서 공통된 key만 뽑아 나열했을 때 그 순서가 같은지를 확인한다. 앞서 말한 O(n²)의 정체가 바로 이 함수다.

```js
const chkDiffLoopOrder = (newWDom, originalWDom) => {
  // 새 자식 중 이전에도 있던 것만 추린다
  const newChildren = [...newWDom.children].filter(item =>
    origChildren.find(o => getKey(item) === getKey(o)));

  // 이전 자식 중 새것에도 있는 것만 추린다
  const filteredChildren = origChildren.filter(item =>
    newChildren.find(n => getKey(item) === getKey(n)));

  // 두 교집합을 나란히 놓고 순서가 같은지 본다
  return filteredChildren.length === newChildren.length &&
         filteredChildren.every((item, i) => getKey(item) === getKey(newChildren[i]));
};
```

`filter` 안에서 다시 `find`를 돌리는 모양이 두 번 반복되는 게 보일 것이다. 리스트가 커질수록 이 비교가 제곱으로 늘어난다.

둘째는 그 검사 결과를 담아두는 표식인 `'L'` 타입이다. 순서가 유지된다고 판정되면 리스트 노드에 `'L'`을 찍어두고, 자식들은 렌더 단계에서 이 표식을 보고 재삽입을 건너뛴다.

```js
const typeSortedUpdate = (newWDom) => {
  typeUpdate(newWDom);            // 내용 갱신
  ...
  if (parentWDom.nr !== 'L') {    // ← 부모에 'L' 이 찍혀 있으면 여기 안 들어온다
    typeAdd(newWDom, ...);        // 재삽입
  }
};
```

정리하면 `'L'`은 **"이 리스트는 재정렬이 필요 없다"는 예/아니오 판정을 담아두는 표식**이었다. `chkDiffLoopOrder`가 묻고, `'L'`이 답을 들고 다니고, 렌더 단계가 그 답을 보고 움직일지 말지를 정하는 구조다.

이 구조가 나중에 어떻게 통째로 사라지는지가 이 글의 결말이다.


## LIS 란 무엇인가

LIS(Longest Increasing Subsequence)는 우리말로 "최장 증가 부분 수열"이다. 이름이 거창하지만 뜻은 단순하다. **숫자가 나열된 수열에서, 순서를 지키면서 고를 수 있는 가장 긴 오름차순 묶음**을 찾는 문제다.

### 부분 수열은 연속이 아니어도 된다

여기서 가장 먼저 짚어야 할 것이 "부분 수열(subsequence)"이라는 말이다. 부분 문자열(substring)처럼 딱 붙어 있어야 하는 게 아니다. **중간을 건너뛰어도 되고, 왼쪽에서 오른쪽으로 가는 순서만 지키면 된다.**

```
수열:  [3, 1, 4, 1, 5, 9, 2, 6]
        -  ✓  ✓  -  ✓  -  -  ✓
              1  4     5        6   ← [1, 4, 5, 6] 길이 4
```

`1 → 4 → 5 → 6`은 원래 수열에서 띄엄띄엄 떨어져 있지만, 등장 순서를 뒤집지 않았고 값이 계속 커지므로 유효한 증가 부분 수열이다. 이런 묶음 중 가장 긴 것이 LIS이고, 위 수열에서는 길이 4가 답이다.

여기까지가 LIS 라는 알고리즘 자체에 대한 설명이다. 내가 이 알고리즘을 처음 알았을 때 곧바로 내 가상돔을 떠올린 이유는, **이것이 DOM 이동 최소화와 정확히 같은 문제**였기 때문이다. 왜 같은 문제인지를 이제 풀어보겠다.

### 리스트 재배치를 숫자 수열로 바꿔보기

키 기반 리스트가 다시 그려질 때 우리가 알고 있는 정보는 두 가지다.

- **새 리스트의 순서** — 사용자가 JSX로 넘긴 그대로이고, 이게 곧 정답이다.
- **각 아이템이 이전 렌더에서 몇 번째였는지** — key로 짝을 맞추면 알 수 있다.

두 번째 정보를 새 리스트 순서대로 나열하면 그게 바로 숫자 수열이 된다. 예를 들어 5개짜리 리스트에서 B와 D의 자리를 바꿨다고 해보자.

```
이전:  [A, B, C, D, E]
        0  1  2  3  4     ← 각자의 이전 위치 번호

이후:  [A, D, C, B, E]
        0  3  2  1  4     ← 새 순서대로 이전 위치 번호를 적으면 이런 수열이 된다
```

`[0, 3, 2, 1, 4]`. 이 수열 하나에 "무엇이 어떻게 어긋났는가"가 전부 담겨 있다.

### 증가 = 상대 순서가 이미 맞다는 뜻

이제 핵심이다. 이 수열에서 **값이 증가하는 두 아이템은 서로 자리를 바꿀 필요가 없다.**

왜 그런가. 아이템 X와 Y가 있고, 새 리스트에서 X가 Y보다 왼쪽에 있다고 하자. 그런데 이전 위치 번호도 X가 더 작다면(= 이전에도 X가 왼쪽에 있었다면), 이 둘의 앞뒤 관계는 처음부터 지금까지 그대로다. 굳이 어느 하나를 뽑아서 옮길 이유가 없다.

반대로 값이 줄어드는 쌍은 앞뒤가 뒤집힌 것이므로 둘 중 하나는 반드시 움직여야 한다.

그러니까 **증가 부분 수열에 속한 아이템들은 서로서로 전부 순서가 맞아떨어지는 무리**다. 이들을 기둥처럼 제자리에 박아두고, 나머지만 그 사이에 끼워 넣으면 리스트가 완성된다.

위 예시에서 `[0, 3, 2, 1, 4]`의 증가 부분 수열을 찾아보면 `[0, 1, 4]`(A, B, E) 같은 것들이 길이 3으로 최대다. 즉 5개 중 3개는 그대로 두고 **2개만 움직이면 된다.** 실제로 우리가 바꾼 것도 B와 D 두 개뿐이니 정확히 들어맞는다.

기존 내 알고리즘이라면 이 상황에서 순서가 어긋났다고 판단하고 5개 전부를 다시 꽂았을 것이다. 리스트가 1만 개였다면 2개 바꾸자고 1만 번의 DOM 이동을 하는 셈이다.

### 왜 하필 "가장 긴" 것인가

제자리에 남길 수 있는 아이템이 많을수록 움직일 아이템이 줄어든다. 그래서 아무 증가 수열이나가 아니라 **가장 긴** 것을 찾아야 한다.

```
이동 횟수 = 전체 개수 − LIS 길이
```

이 한 줄이 LIS가 "최소 이동"을 보장하는 이유다. 그리고 이건 단순히 적당히 좋은 답이 아니라 **수학적으로 증명된 최솟값**이다. LIS보다 많은 아이템을 제자리에 남기는 방법은 존재하지 않는다. 남겼다면 그건 LIS보다 긴 증가 수열을 찾았다는 뜻이고, 최장이라는 정의에 모순이기 때문이다.

덧붙여, 순서가 전혀 바뀌지 않은 경우를 이 공식에 넣어보면 재미있다. 수열이 처음부터 증가 상태이므로 LIS는 전체가 되고, 이동 횟수는 0이 된다. **기존 알고리즘이 `'L'` 판정으로 처리하던 "아무것도 안 움직여도 되는" 케이스가 LIS 안에 그대로 포함되는 것이다.** 내가 따로 만들어두었던 순서 검사 로직을 지울 수 있었던 이유가 여기에 있다.

### 어떻게 빠르게 구하는가

가장 단순한 방법은 각 위치마다 앞쪽을 전부 돌아보며 "나보다 작은 값 중 가장 긴 것"을 찾는 것이다. 이건 O(n²)라서, 애초에 O(n²)를 없애려고 시작한 작업에 쓸 수는 없다.

다행히 이진 탐색을 쓰는 O(n log n) 방법이 잘 알려져 있다. 아이디어는 `tails`라는 배열 하나를 굴리는 것인데, **"길이가 k인 증가 수열을 만들 때 마지막 값이 될 수 있는 가장 작은 수"**를 계속 갱신해 나간다. 꼬리가 작을수록 뒤에 더 많은 수를 이어붙일 수 있으니 유리하다는 발상이다.

`[0, 3, 2, 1, 4]`를 왼쪽부터 한 칸씩 넣어보면 이렇게 흘러간다.

```
0  →  tails = [0]           비어 있으니 그냥 추가
3  →  tails = [0, 3]        마지막(0)보다 크니 뒤에 추가
2  →  tails = [0, 2]        3보다 작다. 2 이상인 첫 값(3)을 2로 교체
1  →  tails = [0, 1]        2보다 작다. 1 이상인 첫 값(2)을 1로 교체
4  →  tails = [0, 1, 4]     마지막(1)보다 크니 뒤에 추가

→ tails 의 길이 3 이 곧 LIS 의 길이
```

값이 계속 커지는 동안에는 뒤에 붙이기만 하면 되고, 작은 값이 나올 때만 이진 탐색으로 교체할 자리를 찾는다. 그래서 순서가 유지되는 흔한 경우에는 사실상 O(n)으로 지나간다.

### 함정: 길이는 맞지만 원소는 틀릴 수 있다

구현할 때 반드시 알아야 할 함정이 하나 있다. **`tails` 배열은 LIS의 길이는 정확히 알려주지만, 그 내용물이 실제 LIS인 것은 아니다.**

```
수열:  [1, 3, 5, 2]

1  →  tails = [1]
3  →  tails = [1, 3]
5  →  tails = [1, 3, 5]
2  →  tails = [1, 2, 5]     3보다 작으니 3을 2로 교체

tails = [1, 2, 5] 인데... 원본에서 5는 2보다 앞에 있다.
[1, 2, 5]는 애초에 만들 수 없는 조합이다. 진짜 LIS는 [1, 3, 5].
```

`tails`는 어디까지나 "각 길이별 최선의 꼬리 후보"를 적어둔 메모지일 뿐이라, 중간에 덮어써진 값들 때문에 실제로 존재하지 않는 조합이 남을 수 있다. 길이만 필요한 문제라면 여기서 끝내도 되지만, 우리는 **어느 아이템을 제자리에 둘지 그 목록 자체가 필요하다.**

그래서 실제 구현에서는 `prev` 배열을 하나 더 둔다. 각 원소를 넣을 때 "나는 어떤 원소 뒤에 붙었다"는 발자국을 남겨두고, 마지막에 그 발자국을 거꾸로 따라가며 진짜 LIS를 복원하는 것이다. Vue 3의 `getSequence` 구현에도 이 역추적 코드가 들어 있고, 내가 작성한 코드도 같은 구조를 따랐다.

정리하면 이렇다.

- 새 리스트의 아이템들이 **이전에 몇 번째였는지**를 수열로 만든다
- 그 수열의 **LIS를 구한다** → 제자리에 둘 아이템 목록
- **LIS에 없는 아이템만** 실제로 DOM에서 옮긴다

이제 이 원리를 실제 가상돔 코드에 어떻게 녹였는지 살펴보자.


## LIS를 실제 내가만든 가상돔에 적용하기

LIS 를 적용하기 위해서는 기존의 키기반 리스트들와 인덱스 순서가... 새로 만들어진 키기반의 리스트들의 순서가 틀어질 경우 각각의 아이템들이 기존에 어떤 순서상에 위치했는지 알아야지 tails 배열을 만들 수 있다.

그러므로 새로운 가상돔이 만들어지는 단계에서 같은키의 아이템이 기존에는 어떤 순서를 갖았었는지 인덱스 정보를 가지고 있어야 한다. 그래서 기존 체계위에 oi 라는 속성을 추가해서 기존키의 인덱스를 기억시켰다.

### 짝을 찾으면서 동시에 순서를 기억한다

`oi`를 심어야 할 위치는 명확했다. 어차피 key로 짝을 맞추는 코드가 이미 있으니, 짝을 찾는 그 순간에 "너는 원래 몇 번째였다"를 같이 적어주면 된다.

```js
const diffLoopChildren = (newWDom, originalWDom) => {
  const origChildren = originalWDom.children || [];

  // 1) 기존 자식들을 key → 인덱스 맵으로 만든다
  const keyMap = new Map();
  origChildren.forEach((item, index) => {
    const key = getKey(item);
    if (!keyMap.has(key)) keyMap.set(key, index);
  });

  // 2) 새 자식들을 돌면서 맵에서 짝을 찾는다
  const remaked = (newWDom.children || []).map(item => {
    const key = getKey(item);
    const origIndex = keyMap.get(key);
    const matched = origIndex !== undefined;

    if (matched) keyMap.delete(key);   // 한 번 쓴 key 는 지운다

    const child = makeNewWDomTree(
      item,
      matched ? origChildren[origIndex] : undefined
    );
    if (matched) child.oi = origIndex;  // ★ 이전 순서를 기억시킨다

    return child;
  });

  // 3) 맵에 남은 것들 = 이번에 사라진 아이템
  return [remaked, [...keyMap.values()].map(index => origChildren[index])];
};
```

그런데 여기서 뜻밖의 소득이 있었다. 기존 코드는 새 아이템마다 `find`로 이전 배열을 훑고, 짝이 되면 `splice`로 빼내는 방식이었다. 이게 앞서 말한 O(n²)의 한 축이었다.

**그런데 "인덱스를 기억해야 한다"는 새 요구사항 때문에 어차피 `key → 인덱스` 맵을 만들어야 했고, 맵을 만들고 나니 `find`와 `splice`가 자연스럽게 사라졌다.** 조회는 O(1)이 되고, 다 쓴 key는 `delete`로 지우면 그만이라 배열을 밀어낼 일도 없다. 맵에 남은 것들이 곧 삭제 대상이라는 구조도 그대로 유지된다.

LIS를 적용하려고 손을 댄 것인데 매칭 단계의 복잡도까지 같이 내려간 셈이다.

### diff 는 기록만, 판단은 render 에서

여기서 한 가지 원칙을 정했다. **diff 단계는 `oi`를 기록만 하고, 그걸로 무엇을 할지는 판단하지 않는다.**

기존 구조에서 diff 단계는 `chkDiffLoopOrder`를 호출해 "재정렬이 필요한가"까지 스스로 판단하고 있었다. 하지만 LIS 계산은 리스트 자식 전체를 한꺼번에 놓고 봐야 하는 일이고, 그 결과로 실제 DOM을 옮기는 것도 render 쪽 일이다. 판단과 실행이 붙어 있는 게 자연스럽다.

그래서 역할을 이렇게 나눴다.

```
diff   : key 로 짝 맞추기 → 각 자식에 oi 기록
render : oi 들을 모아 LIS 계산 → 누가 움직일지 결정 → 실제 DOM 이동
```

`oi`는 이 둘을 잇는 다리 역할만 한다. 덕분에 diff 쪽은 오히려 코드가 줄었다.

### LIS 에게 "누가 가만히 있어도 되는지" 묻는다

render 단계에서는 자식들을 왼쪽에서 오른쪽으로 한 번 훑으며 두 가지 일을 한다. 내용을 갱신하고, 동시에 LIS에 넣을 수열을 모은다.

```js
children.forEach((item, index) => {
  if (item.nr === 'A' || item.nr === 'S') {
    created[index] = wDomToDom(item);   // 엘리먼트만 만들어 둔다 (삽입은 나중에)
  } else if (item.nr === 'T') {
    typeUpdate(item);                   // 내용만 갱신 — 재삽입하지 않는다
  } else {
    wDomUpdate(item);
  }

  if (item.oi !== undefined && created[index] === undefined) {
    matchedIndexes.push(index);
    oiSeq.push(item.oi);                // ★ LIS 입력 수열
  }
});

// 가만히 둬도 되는 아이템들
const stay = new Set(getLisPositions(oiSeq).map(pos => matchedIndexes[pos]));
```

눈여겨볼 부분은 **새로 만든 엘리먼트를 바로 꽂지 않고 `created` 배열에 담아만 둔다**는 점이다. 기존에는 만들자마자 DOM에 삽입했는데, 그러면 "어디에 꽂을지"를 그 자리에서 계산해야 한다. 생성과 삽입을 떼어놓으니 삽입 위치는 뒤에서 한꺼번에 결정할 수 있게 됐다.

그리고 `'T'` 타입 자식은 `typeUpdate`로 내용만 갱신한다. 예전에는 여기서 곧바로 재삽입까지 했지만, 이제 **이동 여부는 LIS가 결정**하므로 손대지 않는다.

### 그런데 "어디에" 꽂을 것인가

LIS는 **누가** 움직여야 하는지 알려주지만 **어디에** 꽂아야 하는지는 알려주지 않는다. 이건 별개의 문제고, 사실 여기서 한 번 더 막혔다.

DOM에서 위치를 지정하려면 `insertBefore(넣을것, 기준점)` 형태로 "내 오른쪽에 올 첫 엘리먼트"가 필요하다. 기존 코드는 아이템마다 자기 오른쪽 형제들을 훑어서 이 기준점을 찾았는데, 아이템이 n개면 이 탐색이 n번 반복되니 이것 역시 O(n²)였다.

해결책은 **오른쪽에서 왼쪽으로 순회하는 것**이었다.

```js
let anchor = 리스트 전체의 다음 형제 엘리먼트;

for (let i = children.length - 1; i >= 0; i--) {
  const item = children[i];
  const isNew = created[i] !== undefined;
  const needMove = item.oi !== undefined && !isNew && !stay.has(i);

  if (isNew || needMove) {
    parentEl.insertBefore(isNew ? created[i] : item.el, anchor || null);
  }

  // 내 엘리먼트가 곧 왼쪽 이웃의 기준점이 된다
  const first = findChildFragmentNextElement([item]);
  if (first) anchor = first;
}
```

원리는 단순하다. 어떤 아이템의 기준점은 결국 "바로 오른쪽 아이템의 엘리먼트"인데, 오른쪽부터 처리하면 그 값이 이미 손에 들려 있다. 각 아이템은 자기 차례에 `anchor`를 받아 쓰고, 자기 엘리먼트를 `anchor`에 넣어 왼쪽 이웃에게 넘겨준다. **탐색 없이 변수 하나를 굴리는 것으로 끝난다.**

LIS에 속한 아이템(`stay`)은 삽입을 건너뛰고 `anchor`만 갱신하고 지나간다. 자기는 안 움직이지만, 왼쪽 이웃에게는 여전히 유효한 기준점이기 때문이다.

거꾸로 왼쪽부터 처리하면 이 방식이 성립하지 않는다. 왼쪽 아이템을 꽂아봐야 그건 오른쪽 아이템의 기준점이 될 수 없어서, 결국 매번 오른쪽을 다시 탐색해야 한다. 기존 코드가 O(n²)였던 이유가 바로 이 방향 문제였다.

여기에 하나 더, **리스트 끝에 새 아이템이 연달아 추가되는 경우**는 아예 기준점 자체가 필요 없다. 맨 뒤에 붙이는 것이니 순서대로 `appendChild` 하면 그만이다. 목록 끝에 항목을 추가하는 건 실무에서 가장 흔한 패턴이라 이 경우만 따로 빠른 길로 빼두었다.

정리하면 리스트 하나를 갱신할 때 이런 순서로 흘러간다.

```
1. 왼 → 오 : 내용 갱신 + 신규 엘리먼트 생성 + oi 수집
2.          : LIS 계산 → 가만히 둘 아이템 확정
3. 꼬리     : 끝에 몰린 신규 아이템은 그냥 append
4. 오 → 왼 : 신규 + 이동 대상만 anchor 앞에 삽입
```


## 왜 새로 변경된 알고리즘에서는 L 타입이 필요없는가?

기존 구현에는 `'L'`이라는 렌더 타입이 있었다. 이름은 `LOOP_CHILDREN_NOT_SORTED_UPDATE`, 뜻은 **"이 리스트는 재정렬이 필요 없는 업데이트다"**였다.

동작은 이랬다. diff 단계에서 `chkDiffLoopOrder`로 공통 key들의 순서가 유지되는지 검사하고, 유지되면 리스트 노드에 `'L'`을 찍는다. 그러면 render 단계에서 자식들이 이 표식을 보고 재삽입을 건너뛴다.

```js
const typeSortedUpdate = (newWDom) => {
  typeUpdate(newWDom);            // 내용 갱신
  ...
  if (parentWDom.nr !== 'L') {    // ← 부모가 'L' 이면 여기 안 들어옴
    typeAdd(newWDom, ...);        // 재삽입
  }
};
```

즉 `'L'`은 **"전원 제자리에 둬도 되는가?"라는 예/아니오 질문에 대한 답**을 담아두는 표식이었다.

### 예/아니오 질문이 개수 질문으로 바뀌었다

LIS가 답하는 질문은 이것보다 일반적이다.

```
기존 :  "전원 제자리에 둘 수 있는가?"        → 예 / 아니오
LIS  :  "몇 명이나 제자리에 둘 수 있는가?"   → 0 ~ n
```

그리고 **기존 질문의 "예"는 새 질문의 답이 n(전체)인 경우와 정확히 같다.**

순서가 전혀 바뀌지 않았다면 `oi` 수열은 `[0, 1, 2, 3, 4]`처럼 처음부터 증가 상태다. 그러면 LIS는 전체를 포함하고, `stay` 집합에 모든 아이템이 들어가고, 배치 패스에서 아무도 `insertBefore` 되지 않는다. **`'L'` 분기가 하던 일이 그대로 재현된다.** 별도의 표식도, 사전 검사도 없이 말이다.

특수 케이스를 위해 따로 만들어둔 분기가, 일반해를 구현하고 나니 그 안에 흡수되어 버린 것이다.

### 표식 하나를 지웠더니 코드가 연쇄로 줄었다

`'L'`을 부여하는 곳이 사라지자 그것을 참조하던 코드도 전부 죽은 코드가 됐다. 실제로 지운 목록은 이렇다.

- `chkDiffLoopOrder` 함수 전체 (25줄, O(n²) 짜리)
- `addReRenderTypeProperty` 안의 `'L'` 판정 블록
- `RenderType` 타입 정의에서 `'L'`
- 렌더 핸들러 맵의 `L: typeUpdate` 항목
- `typeSortedUpdate`의 `parentWDom.nr !== 'L'` 분기
- `typeAdd`의 기준점 계산에 있던 `parentWDom.nr !== 'L'` 조건

내가 만든 가상돔은 코어를 최소 용량으로 유지하는 것이 중요한 목표라, 상수 하나가 늘어나는 것도 신경 쓰는 편이다. LIS 알고리즘을 넣으면 코드가 늘어날 것이라 각오하고 시작했는데, 위 목록만큼이 상쇄되어 최종 증가폭은 생각보다 크지 않았다.

정확히 말하면 알고리즘 자체는 더 정교해졌는데 **분기는 오히려 줄었다.** "순서가 같은가?"를 따로 묻지 않으니 그 답에 따라 갈라지던 길도 필요가 없어졌기 때문이다.

돌이켜보면 이게 좋은 신호였던 것 같다. 특수 케이스를 위한 예외 분기가 사라지고 하나의 일반적인 계산으로 정리된다면, 대체로 그 방향이 맞다.

