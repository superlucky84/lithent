import { h, Fragment } from '@/wDom';
import { render } from '@/render';
import {
  mounted,
  updated,
  unmount,
  makeData,
  makeDataStore,
  useDataStore,
  makeRef,
} from '@/hook';

export {
  h,
  Fragment,
  render,
  mounted,
  updated,
  unmount,
  makeData,
  makeDataStore,
  useDataStore,
  makeRef,
};
